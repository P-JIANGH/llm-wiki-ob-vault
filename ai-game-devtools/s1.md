---
title: s1
created: 2026-04-14
updated: 2026-04-14
type: entity
tags: [llm, ai, model, reasoning, test-time-scaling, open-source]
sources: [raw/articles/ai-game-devtools/s1.md]
---

# s1: Simple Test-Time Scaling

## 概述

s1 是由 SimpleScaling 团队发布的 LLM 推理增强方法，通过**测试时扩展（test-time scaling）** 和 **budget forcing** 技术，仅用 1,000 个样本微调即实现了与 OpenAI o1-preview 相当的推理能力。核心论文 [arXiv:2501.19393](https://arxiv.org/abs/2501.19393)。

## 关键事实

| 属性 | 值 |
|------|-----|
| 基础模型 | Qwen2.5-32B |
| 微调版本 | s1-32B → s1.1-32B |
| 训练数据 | s1K-1.1 (Gemini/r1 traces) |
| 推荐硬件 | 16×H100 (2节点) |
| 许可证 | MIT |

## 核心技术

### Test-Time Scaling（测试时扩展）
通过在推理阶段分配更多计算预算（更多 token）来提升模型性能，而非仅依赖预训练/微调。不同于传统的 model scaling，test-time scaling 在推理时动态分配计算资源。

### Budget Forcing（预算强制）
在 vLLM 推理时：
1. 设置 `MAX_TOKENS_THINKING` 上限
2. 当模型输出 `<|im_start|><|im_end|>`（end-of-thinking）时主动忽略
3. 追加 "Wait" 字符串，强制模型继续推理
4. 最终生成答案时切换到 `<|im_end|>` stop token

这样可以强迫模型在某些错误分叉点重新思考，例如 "How many r in raspberry" 这个问题，模型初始会漏算，budget forcing 强制其重新清点。

### 推理引擎
- **vLLM**：生产级推理，支持 tensor_parallel_size=2，`max_tokens=32768`
- **Transformers**：直接 HF 加载，方便研究
- **Budget Forcing**：需配合 vLLM，通过循环忽略 stop token 实现

## 架构与模块

### `train/`
- `sft.py`：主 SFT 训练脚本，基于 TRL 的 `SFTTrainer`
- `sft.sh` / `sft_multinode.sh`：单节点/多节点启动脚本
- FSDP 配置：支持 Qwen 的 CPU offload 和标准 GPU 配置
- s1.1 使用 `block_size=20000` 防止 OOM

### `eval/`
- 克隆 lm-evaluation-harness（commit `4cec66e4`）并修改
- `commands.sh`：所有评估命令（AIME24 等）
- `compute_sample_stats.py`：统计推理 token 消耗、平均长度等

### `data/`
完整数据生成流水线：
1. `collect_data.py` → 收集问题
2. `fix_gpqa.py` → 修复 GPQA 数据
3. `add_aime.py` → 加入 AIME 数学题
4. `gemini.py` → Gemini 生成 reasoning traces
5. `bulk_inference.py` → Qwen 生成答案
6. `featurization.py` → 特征工程
7. `filter.ipynb` → 最终过滤筛选

## 与同类工具的差异

- **vs o1-preview**：s1 用极简方法（1K 样本 + budget forcing）达到相近效果，o1 是闭源黑盒
- **vs R1 (DeepSeek)**：s1 基于 Qwen2.5，R1 基于 DeepSeek；s1 强调 test-time scaling 的可复现性
- **vs 传统 SFT**：传统 SFT 只训练模型权重，s1 同时展示了推理时干预（budget forcing）的威力

## 相关链接

- 论文：https://arxiv.org/abs/2501.19393
- 模型：https://huggingface.co/simplescaling/s1.1-32B
- 数据：https://huggingface.co/datasets/simplescaling/s1K-1.1

## 关联

- [[DeepSeek-R1]] — r1 reasoning traces 是 s1.1 的训练数据来源之一
- [[Qwen2]] — s1 基于 Qwen2.5-32B 基座模型
- [[llm.c]] — test-time scaling 是另一种扩展范式（vs pre-training scaling）
